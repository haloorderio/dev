backend:
  name: github
  repo: hikammn/hikammn.github.io
  branch: main
  site_domain: relaxed-blancmange-bf2a4b.netlify.app

media_folder: "assets/img/upload" # Media files will be stored in the repo under images/uploads
public_folder: "/assets/img/upload" # The src attribute for uploaded media will begin with /images/uploads

collections:
  - name: "produk" # Used in routes, e.g., /admin/collections/blog
    label: "Produk" # Used in the UI
    folder: "_posts" # The path to the folder where the documents are stored
    create: true # Allow users to create new documents in this collection
    slug: "YYYY-MM-DD-title.md" # Filename template, e.g., YYYY-MM-DD-title.md
    fields: # The fields for each document, usually in front matter
      - {label: "Layout", name: "layout", widget: "hidden", default: "post"}
      - {label: "Author", name: "author", widget: "string"}
      - {label: "Title", name: "title", widget: "string"}
      - {label: "Publish Date", name: "date", widget: "datetime"}
      - {label: "Featured Image", name: "thumbnail", widget: "image"}
      - {label: "Category", name: "category", widget: "string", default: "jekyll"}
      - {label: "Summary", name: "summary", widget: "string"}
      - {label: "Body", name: "body", widget: "markdown"}